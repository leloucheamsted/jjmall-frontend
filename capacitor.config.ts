import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.kachabazarstore.app",
  appName: "kachabazar-store",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
